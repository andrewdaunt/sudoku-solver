import React from 'react';
import styles from '../styles/SudokuHeader.module.css';

function Title(){
    // returns a title header
    return <h1 className={styles.title}>Sudoku Solver</h1>;  
};

export default Title;
